import { ReactNode } from "react"
import "./card.css"

type Card = {
    img?: ReactNode
    title: string
    categories: Array<string>
    score: string
    scoreNumber: string
    delivery: string
    deliveryPrice : string
}
export const Card = ({ }) => {
    return (

    )
}
